# deps / build
node_modules/
.next/
out/
*.tsbuildinfo

# env — never commit real keys (I2). The template is .env.example.
.env
.env.*
!.env.example

# generated reports (regenerable; may carry clinic-specific demo data)
reports/*.md
reports/*.json
reports/tts-samples/
!reports/.gitkeep

# misc
.DS_Store
npm-debug.log*
pnpm-debug.log*
